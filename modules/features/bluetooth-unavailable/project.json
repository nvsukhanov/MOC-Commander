{
  "name": "feature:bluetooth-unavailable",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "modules/features/bluetooth-unavailable/src",
  "prefix": "lib",
  "tags": ["scope:feature"],
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "modules/features/bluetooth-unavailable/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "modules/features/bluetooth-unavailable/**/*.ts",
          "modules/features/bluetooth-unavailable/**/*.html",
          "modules/features/bluetooth-unavailable/package.json"
        ]
      }
    }
  }
}
